<Window x:Class="SalesManagementSystem.WPF.CategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Category Details" Height="220" Width="350">
    <Grid Margin="10,10,10,0" Height="184" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Category Name:" Grid.Row="0"/>
        <TextBox Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Margin="120,0,0,0"/>
        <TextBlock Text="Description:" Grid.Row="1"/>
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="120,0,0,0"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Margin="5" Click="Save_Click" Height="29"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click" Height="30"/>
        </StackPanel>
    </Grid>
</Window>

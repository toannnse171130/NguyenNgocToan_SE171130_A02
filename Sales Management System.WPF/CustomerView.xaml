<Window x:Class="SalesManagementSystem.WPF.CustomerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SalesManagementSystem.BusinessLogic"
        Title="Customer Management" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" Margin="10,40,10,50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="50"/>
                <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="150"/>
                <DataGridTextColumn Header="Contact Name" Binding="{Binding ContactName}" Width="120"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Add" Width="80" Margin="5" Click="AddCustomer_Click"/>
            <Button Content="Edit" Width="80" Margin="5" Click="EditCustomer_Click"/>
            <Button Content="Delete" Width="80" Margin="5" Click="DeleteCustomer_Click"/>
        </StackPanel>
    </Grid>
</Window>

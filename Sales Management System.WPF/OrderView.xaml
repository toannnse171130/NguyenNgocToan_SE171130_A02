<Window x:Class="SalesManagementSystem.WPF.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SalesManagementSystem.BusinessLogic"
        Title="Order Management" Height="500" Width="900">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}" AutoGenerateColumns="False" Margin="10,40,10,50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" Width="80"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding Customer.CompanyName}" Width="150"/>
                <DataGridTextColumn Header="Employee" Binding="{Binding Employee.Name}" Width="120"/>
                <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Add" Width="80" Margin="5" Click="AddOrder_Click"/>
            <Button Content="Edit" Width="80" Margin="5" Click="EditOrder_Click"/>
            <Button Content="Delete" Width="80" Margin="5" Click="DeleteOrder_Click"/>
        </StackPanel>
    </Grid>
</Window>
